<div dir="rtl">

# מערכת אוטומציה לניהול פרוטוקולים והקלדות - חבר הון אנושי

מסמך זה מרכז את ההסבר המעודכן על כלל רכיבי מערכת האוטומציה לניהול דיונים, הקלדות והקצאת מתמללים.

## מטרת המערכת
ייעול ואוטומציה של תהליך ניהול מאות קבצי תמלול (פרוטוקולים) ביום – כולל ספירת מילים, חילוץ נתוני דיונים, עדכון טבלאות אקסל מורכבות (כולל גוגל שיטס), ושליחת התוצרים לאנשי הקשר המתאימים בבתי המשפט בזמן אמת, וכן הקצאת עבודה למתמללים.

## רכיבים וסקריפטים עיקריים במערכת

### 1. ניהול תורים והקצאות (מנוע הליבה)
* **`transcription_queue_manager.py`**: הסקריפט המרכזי לניהול תורי התמלול של אזור דרום וירושלים. אחראי על:
  - סינון דיונים פתוחים מאקסל "שרות א חדש".
  - חישוב תאריכי יעד (SLA) לכל דיון.
  - חישוב קיבולת עבודה יומית של מתמללים מתוך קבצי "איושים" ושכר שכירים.
  - אלגוריתם הקצאה למתמללים לפי זמינות או ניתוב קשיח.
  - העתקת קבצי שמע (MP3, WAV וכו') לכונן O ו-F של המתמללים.
  - שליחת מייל עדכון למתמלל על העבודה שהוקצתה.

### 2. קליטת הזמנות ומיילים
* **`orders_processor.py`**: מערכת לקריאת מיילים נכנסים מתיבת ההזמנות הייעודית, חילוץ טפסי הזמנה (PDF), והזנת הפרטים לאקסל המרכזי למניעת הזנה ידנית כפולה.
* **`fast_email_sender.py`**: מנוע חכם לשליחת תוצרים (פרוטוקולים מוכנים) לאנשי הקשר בבתי המשפט. כולל מערכת תורים - דיונים שמוכנים בלילה נשמרים בתור ונשלחים אוטומטית בבוקר.

### 3. עבודה מול קבצים וטבלאות (Google Sheets & Excel)
* **`excel_queue_manager.py`**: ניהול גישה כתיבה/קריאה לקבצי אקסל במקביל כדי למנוע נעילות או השחתת מידע.
* **`gsheets_utils.py`**: סנכרון נתונים דו-כיווני בין קבצי האקסל המקומיים למערכת Google Sheets, מאפשר מעקב ושקיפות בזמן אמת.
* **`config_drive_paths.py`**: איתור נתיבי רשת דינמיים (כמו תיקיית ההקלטות של החודש הנוכחי או מיקום קובץ שירות א').

### 4. בינה מלאכותית ועיבוד טקסט
* **`llm_utils.py`**: חיבור למודל הבינה המלאכותית (Gemini). משמש לחילוץ מידע חכם מטקסטים לא מובנים (זיהוי מספרי הליכים ושופטים מפרוטוקולים או מיילים) ברמת דיוק גבוהה.
* **`word_format_checker.py`**: מנגנון אוטומטי לבדיקת תקינות העיצוב בקבצי Word (גופן דוד 12, יישור ימינה, רווח 1.5, כניסה תלויה וכו') לפני שליחתם ללקוח.
* **`transcribe_to_protocol_final.py` / `ai_real_dry_run.py`**: סקריפטים לעיבוד המסמכים, ספירת המילים המדויקת וחילוץ נתונים מתוך תוכן הפרוטוקול.

### 5. שרת אישורים, בקרה ודשבורד
* **`approval_server.py`**: שרת מקומי המאפשר התערבות אנושית ואישור של פעולות במקרים גבוליים, דרך ממשק דפדפן (למשל, זיהוי כפול של שופט).
* **`run_nightly_job.py`**: סקריפט תחזוקה ובקרה שרץ מדי לילה. מפיק דוח סיכום יומי, מתריע על חוסרים (תיקים ללא שמע/פרוטוקול), ומוודא שכל הדיונים טופלו. הדוח נשלח בפורמט HTML.
* **`dashboard_live.html` / `transcription_dashboard.html`**: ממשק דשבורד ויזואלי להצגת מצב התיקים, הקצאות המתמללים והתקדמות העבודה היומית במבט על, ישירות בדפדפן.

### 6. עזרי מערכת 
* **`sys_process_utils.py`**: ניהול תהליכים ומניעת קריסות (סגירת תהליכי רקע תקועים של אקסל/וורד).

---
*מערכת זו חוסכת מאות שעות עבודה ידניות בחודש, מונעת טעויות אנוש, ומנהלת את מערך ההקלדות של החברה בצורה חכמה ומדויקת מתחילתו ועד סופו.*

</div>
